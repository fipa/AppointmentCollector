<%= form_for(@calendar, html: {role: "form", class:"form-horizontal"}) do |f| %>
  <% if @calendar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@calendar.errors.count, "error") %> prohibited this calendar from being saved:</h2>

      <ul>
      <% @calendar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: "col-sm-2 control-label" %>
	<div class="col-sm-10">
		<%= f.text_field :name, class: "form-control" %>
	</div>
  </div>
  <div class="form-group">
    <%= f.label :calendar_key, class: "col-sm-2 control-label" %>
	<div class="col-sm-10">
	    <%= f.text_field :calendar_key %>
	</div>
  </div>
  <div class="form-group">
    <%= f.label :comments, class: "col-sm-2 control-label" %>
	<div class="col-sm-10">
	    <%= f.text_area :comments %>
	</div>
  </div>
  <div class="form-group">
	<div class= "col-sm-4">&nbsp;</div>
	<%= f.submit "Crear", class: "btn col-sm-4" %>
	<div class= "col-sm-4">&nbsp;</div>  
</div>
<% end %>
